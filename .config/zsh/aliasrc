# Default to vim
alias vi=vim

# Colorized outputs
alias ls="ls -al --color=auto"

# k8s
alias k=kubectl --context $KUBE_CTX
alias kubectl=kubectl --context $KUBE_CTX

# terraform
alias tf=terraform

# git
alias g=git
